version: "3.8"

services:
  app:
    image: php:8.2-apache
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
      - app_bdd:/var/www/html/bdd
    working_dir: /var/www/html
    command: >
      sh -lc "php scripts/init_demo_dbs.php && apache2-foreground"

volumes:
  app_bdd:
